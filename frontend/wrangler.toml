name = "cti-nlp"
compatibility_date = "2025-09-24"
compatibility_flags = ["nodejs_compat"]

# Point to the actual build output directory from Vite
pages_build_output_dir = "dist/client"

[build]
command = "npm run build"

# Pages configuration
[pages]
compatibility_flags = ["nodejs_compat"]
